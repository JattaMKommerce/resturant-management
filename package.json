{
  "name": "hotel-and-restaurant-platform",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm --prefix frontend install && npm --prefix backend install",
    "build": "npm --prefix frontend install && npm --prefix backend install && npm --prefix frontend run build && node -e \"const fs=require('fs');fs.cpSync('frontend/dist','dist',{recursive:true});\"",
    "start": "cd backend && node server.js",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "start:backend": "cd backend && npm start",
    "db:init": "cd backend && npm run db:init"
  }
}
