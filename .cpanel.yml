---
deployment:
  tasks:
    - export PATH=/opt/cpanel/ea-nodejs22/bin:/usr/local/bin:/usr/bin:$PATH && cd backend && npm install --omit=dev
    - export PATH=/opt/cpanel/ea-nodejs22/bin:/usr/local/bin:/usr/bin:$PATH && cd frontend && npm install && npm run build
    - mkdir -p tmp backend/tmp
    - touch tmp/restart.txt backend/tmp/restart.txt